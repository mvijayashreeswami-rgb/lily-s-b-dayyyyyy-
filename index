<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday BesTEA! 💖</title>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;600;700&family=Indie+Flower&family=Courier+Prime&display=swap" rel="stylesheet">
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: #fff5f7;
            color: #5c4b51;
            font-family: 'Quicksand', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
            padding: 30px 15px;
            overflow-x: hidden;
        }

        /* --- TERMINAL OVERLAY --- */
        #terminal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(11, 15, 25, 0.95);
            z-index: 1000;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .terminal-box {
            background: #121826;
            border: 2px solid #00ffcc;
            border-radius: 8px;
            width: 100%;
            max-width: 500px;
            padding: 25px;
            font-family: 'Courier Prime', monospace;
            color: #00ffcc;
            box-shadow: 0 0 25px rgba(0, 255, 204, 0.4);
        }

        .terminal-box h2 {
            font-size: 1.2rem;
            margin-bottom: 15px;
            color: #fff;
        }

        .terminal-box p {
            font-size: 0.9rem;
            color: #8a99ad;
            margin-bottom: 20px;
        }

        .terminal-btn {
            display: block;
            width: 100%;
            background: #1a233a;
            color: #00ffcc;
            border: 1px solid #00ffcc;
            padding: 10px;
            margin-bottom: 10px;
            cursor: pointer;
            font-family: 'Courier Prime', monospace;
            text-align: left;
            border-radius: 4px;
            transition: 0.2s;
        }

        .terminal-btn:hover {
            background: #00ffcc;
            color: #0b0f19;
            font-weight: bold;
        }

        /* --- MAIN SCRAPBOOK STYLES --- */
        .main-content {
            display: none; /* Hidden until terminal is passed */
            flex-direction: column;
            align-items: center;
            width: 100%;
        }

        .doodle {
            position: absolute;
            font-size: 1.5rem;
            opacity: 0.4;
            animation: float 6s ease-in-out infinite alternate;
            z-index: -1;
        }

        @keyframes float {
            0% { transform: translateY(0px) rotate(0deg); }
            100% { transform: translateY(-20px) rotate(10deg); }
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
        }

        h1 {
            font-family: 'Indie Flower', cursive;
            font-size: 3rem;
            color: #ff6b8b;
            margin-bottom: 10px;
        }

        .header p {
            font-size: 1.1rem;
            color: #8c7882;
        }

        .scrapbook-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
            width: 100%;
            max-width: 900px;
            margin-bottom: 40px;
        }

        .polaroid {
            background: #fff;
            padding: 15px 15px 40px 15px;
            border-radius: 4px;
            box-shadow: 0 8px 20px rgba(255, 107, 139, 0.15);
            transform: rotate(-2deg);
            transition: 0.3s ease;
            position: relative;
        }

        .polaroid:nth-child(even) {
            transform: rotate(2deg);
        }

        .polaroid:hover {
            transform: scale(1.03) rotate(0deg);
            box-shadow: 0 12px 25px rgba(255, 107, 139, 0.25);
            z-index: 10;
        }

        .polaroid img, .polaroid video {
            width: 100%;
            height: 250px;
            object-fit: cover;
            border-radius: 2px;
        }

        .caption {
            font-family: 'Indie Flower', cursive;
            font-size: 1.3rem;
            text-align: center;
            margin-top: 15px;
            color: #5c4b51;
        }

        .tape {
            position: absolute;
            top: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 25px;
            background: rgba(255, 230, 235, 0.8);
            border: 1px dashed #ffb6c1;
        }

        /* --- SOUNDBOARD STYLES --- */
        .soundboard-section {
            background: #fff;
            padding: 25px;
            border-radius: 20px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            text-align: center;
            max-width: 600px;
            width: 100%;
            margin-bottom: 30px;
            border: 2px dashed #ffb6c1;
        }

        .soundboard-section h3 {
            font-family: 'Indie Flower', cursive;
            font-size: 1.8rem;
            color: #ff6b8b;
            margin-bottom: 15px;
        }

        .sound-buttons {
            display: flex;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
        }

        .sound-btn {
            background: #ff6b8b;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 20px;
            cursor: pointer;
            font-family: 'Quicksand', sans-serif;
            font-weight: bold;
            transition: 0.2s;
            box-shadow: 0 4px 10px rgba(255, 107, 139, 0.3);
        }

        .sound-btn:hover {
            background: #ff476f;
            transform: translateY(-2px);
        }

        .footer-note {
            background: #fff;
            padding: 25px 35px;
            border-radius: 20px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            text-align: center;
            max-width: 600px;
            border: 2px dashed #ffb6c1;
        }

        .footer-note h2 {
            font-family: 'Indie Flower', cursive;
            font-size: 2rem;
            color: #ff6b8b;
            margin-bottom: 10px;
        }

        .footer-note p {
            line-height: 1.6;
            font-size: 1rem;
        }
    </style>
</head>
<body>

    <!-- FAKE TERMINAL VERIFICATION POPUP -->
    <div id="terminal-overlay">
        <div class="terminal-box">
            <h2>> SYSTEM SECURITY CHECK</h2>
            <p>Identity confirmation required. Are you actually qualified to view this confidential birthday data?</p>
            <button class="terminal-btn" onclick="passTerminal()">[1] Yes, I play guitar and lift heavy stuff.</button>
            <button class="terminal-btn" onclick="passTerminal()">[2] No, but I brought food.</button>
            <button class="terminal-btn" onclick="passTerminal()">[3] Bypass security (I am the birthday boy).</button>
        </div>
    </div>

    <!-- MAIN WEBSITE CONTENT -->
    <div class="main-content" id="mainContent">

        <div class="doodle" style="top: 5%; left: 10%;">🌸</div>
        <div class="doodle" style="top: 15%; right: 15%; animation-delay: 1s;">✨</div>
        <div class="doodle" style="top: 60%; left: 5%; animation-delay: 2s;">💖</div>
        <div class="doodle" style="bottom: 10%; right: 10%; animation-delay: 1.5s;">🎸</div>

        <div class="header">
            <h1>Happy Birthday to My Favorite Human! 🎉</h1>
            <p>A little digital scrapbook of our chaotic moments together.</p>
        </div>

        <div class="scrapbook-container">
            <!-- Photo 1 -->
            <div class="polaroid">
                <div class="tape"></div>
                <img src="your-photo1.jpg" alt="Memory 1">
                <div class="caption">The guitarist in his natural habitat 🎸</div>
            </div>

            <!-- Photo 2 -->
            <div class="polaroid">
                <div class="tape"></div>
                <img src="your-photo2.jpg" alt="Memory 2">
                <div class="caption">Gym rat behavior 💪</div>
            </div>

            <!-- Video Memory -->
            <div class="polaroid">
                <div class="tape"></div>
                <video controls>
                    <source src="your-video.mp4" type="video/mp4">
                    Your browser does not support the video tag.
                </video>
                <div class="caption">Unhinged footage we can never delete 🎥</div>
            </div>
        </div>

        <!-- SOUNDBOARD SECTION -->
        <div class="soundboard-section">
            <h3>🎵 Emergency Soundboard</h3>
            <p style="font-size: 0.9rem; color: #8c7882; margin-bottom: 15px;">Click to trigger panic audio effects:</p>
            <div class="sound-buttons">
                <button class="sound-btn" onclick="playSound('audio1.mp3')">🎸 Air Guitar Riff</button>
                <button class="sound-btn" onclick="playSound('audio2.mp3')">🎤 Bad Singing</button>
                <button class="sound-btn" onclick="playSound('audio3.mp3')">🚨 Roasting Alert</button>
            </div>
        </div>

        <div class="footer-note">
            <h2>You're pretty cool, I guess.</h2>
            <p>Thanks for being the best partner-in-crime for food, late-night talks, and endless nonsense. Hope your day is as awesome as your guitar solos. Happy Birthday! 🎂✨</p>
        </div>

    </div>

    <script>
        function passTerminal() {
            // Hide terminal and show the actual scrapbook website
            document.getElementById('terminal-overlay').style.display = 'none';
            document.getElementById('mainContent').style.display = 'flex';
        }

        function playSound(filename) {
            // To use audio, just put audio1.mp3, audio2.mp3, etc. in your folder!
            const audio = new Audio(filename);
            audio.play().catch(error => {
                alert("Audio file missing! Drop '" + filename + "' into your folder to make this work.");
            });
        }
    </script>

</body>
</html>
